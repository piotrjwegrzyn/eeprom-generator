package opers

func GeneratePageLow() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage00h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage01h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage02h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage04h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage10h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage11h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage12h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage24h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}

func GeneratePage25h() (page []byte) {
	for i := 0; i < 128; i++ {
		page = append(page, byte(i))
	}
	return
}
